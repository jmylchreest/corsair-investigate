# scimitar-diag environment
EVENTS_LOG=/var/log/scimitar-diag/events.log
HID_LOG_DIR=/var/log/scimitar-diag
ROTATE_MB=100
# daily prune: keep ±window around markers + everything in the grace period
PRUNE_WINDOW_MIN=10
PRUNE_GRACE_HOURS=24
